# ============================================================
# 学生身份证区划代码转换工具 - 运行配置文件
# ------------------------------------------------------------
# 修改后保存，重新启动 app.py 即可生效。
# 生效方式：python app.py
# ============================================================

# 运行端口（1024~65535 整数，低于 1024 通常需要管理员权限）
PORT=5000

# ------------------------------------------------------------
# 系统托盘（仅 Windows 桌面有效；无图形界面的服务器环境会自动回退为直接运行）
# on  = 启动后最小化到托盘，双击图标或点菜单打开主页，可一键退出
# off = 直接前台运行（保留控制台窗口）
TRAY=on

# 启动后是否自动打开浏览器主页
OPEN_BROWSER=on
