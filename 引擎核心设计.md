# 引擎核心设计

> 本文档全面梳理 `district_matcher`（学生身份证区划代码转换工具）的引擎架构、核心源码、配置资源、关键算法与移植要点，便于开发者快速理解全貌并完成移植。

---

## 一、整体架构设计

### 1.1 设计目标

将「身份证号 + 地址文本」转换为标准的省/市/区县三级 12 位行政区划代码。采用**双引擎**策略：

- **引擎①（身份证）**：由身份证前 6 位按 GB/T 2260 层级匹配行政区划代码。受历史代码变更（撤县设市/设区）影响，可能命中已失效的旧代码。
- **引擎②（地址）**：由地址文本解析出省/市/区县名称，再反查区划字典得到现行代码。能正确处理历史更名，是主信息源。

**v2 决策原则**：引擎②为**绝对主引擎**，行政层级 3（区县/县级市）是判定锚点；引擎①降级为**校验器 + 兜底补充器**，仅当地址为空或引擎②无法解析层级 3 时提供区县。归属永远先问引擎②，从架构上杜绝跨引擎省/市/区县拼接。

### 1.2 核心模块划分

系统分为 6 层：

| 层 | 目录 | 职责 |
|----|------|------|
| **配置层** | `config.py` / `config.txt` | 路径、直辖市表、代码补零规则、运行端口/托盘开关 |
| **数据层** | `database/` | SQLite 持久化 + 区划字典内存缓存；表结构定义；数据模型 |
| **引擎层** | `engine/` | 引擎①、引擎②、决策器——纯逻辑，无 IO 副作用 |
| **服务层** | `services/` | 导入、匹配、导出业务编排；连接引擎层与数据层 |
| **工具层** | `utils/` | 地址正则解析、身份证校验 |
| **接入层** | `app.py` / `sdk/` / `templates/` | Flask Web + RESTful API + 系统托盘；Python SDK |

### 1.3 模块依赖关系

```
接入层 (app.py / sdk/district_matcher.py / templates)
   │
   ▼
服务层 (import_service / match_service / export_service)
   │
   ├──────────────┐
   ▼              ▼
引擎层           数据层 (db_manager)
(engine1/        (区划缓存 + 学生/结果读写)
 engine2/
 decider)
   │
   ▼
工具层 (address_parser / validators)
   │
   ▼
配置层 (config)
```

依赖方向严格**自上而下**：接入层→服务层→引擎层/数据层→工具层→配置层。引擎层不直接依赖 Flask、pandas，只依赖 `db_manager`（传入）与 `utils`/`config`，保证可独立测试与移植。

### 1.4 数据流向

#### (1) 初始化流程（区划字典导入）

```
Excel(多sheet) → import_service.import_district_excel
  → _auto_map 识别列 → _build_district_rows 规范化(6位补零/pid推导/层级推导)
  → 缺失省级自动合成(以 sheet 名为省名)
  → db.insert_districts(单次事务) → 失效内存缓存
```

#### (2) 单条匹配流程

```
(id_card, address) → match_service.run_match
  → 证件框取前6位为数字即启动引擎①（兼容6位区划代码/18位身份证）
  → 地址非空即启动引擎②
  → 两框不必都填；均无效（证件前6位非数字且地址为空）→ 标“异常”
  → decider.dual_engine_decide(db, e1或空Engine1Result, e2) → MatchResult
  → (可选)构建 engine1/engine2 过程详情文本（未执行的引擎显示“未执行”）
```

#### (3) 批量处理流程

```
process_batch(db, batch_id)
  → 取该批学生 → delete_results_by_batch(覆盖旧结果)
  → 逐条 run_match(with_detail=True) → 收集 rows
  → insert_results_batch(单次事务提交)  [P02]
  → 返回 stats 统计；可选 on_progress 回调驱动前端进度条
```

#### (4) 导出流程（一进一出）

```
export_service.export_results
  → db.get_results_dataframe(pd.read_sql 一步取数)
  → rename 中文列名 → 写 xlsx/csv(文件名含 batch_id+时间戳)
  → cleanup=True 且整批导出 → db.delete_batch(清学生流水,保留字典)
```

---

## 二、核心源码文件清单

### 2.1 目录结构

```
district_matcher/
├── app.py                     # Flask 主应用(页面/API/托盘)
├── config.py                  # 路径与全局常量
├── config.txt                 # 运行配置(端口/托盘/浏览器)
├── requirements.txt           # 外部依赖
├── README.md
├── data/                      # 区划 Excel 数据目录
├── uploads/                   # 上传/导出临时目录
├── database/
│   ├── __init__.py
│   ├── db_manager.py          # DBManager + Schema
│   └── models.py              # 数据模型 dataclass
├── engine/
│   ├── __init__.py
│   ├── engine1.py             # 引擎①
│   ├── engine2.py             # 引擎②
│   └── decider.py             # 决策器 v2
├── services/
│   ├── __init__.py
│   ├── import_service.py      # 区划/学生导入
│   ├── match_service.py       # 匹配编排
│   └── export_service.py      # 导出
├── utils/
│   ├── __init__.py
│   ├── address_parser.py      # 地址正则解析
│   └── validators.py          # 身份证校验
├── sdk/
│   ├── __init__.py
│   └── district_matcher.py    # 对外 SDK
├── templates/                 # base/init/import/query/result
├── static/css/style.css
└── tests/                     # 单测 + 验证脚本
```

### 2.2 文件职责与关键接口

| 文件 | 职责 | 入口/关键接口 |
|------|------|---------------|
| `config.py` | 路径常量、直辖市映射、12 位代码补零规则、状态枚举、层级名 | 模块级常量 `DB_PATH`/`MUNICIPALITIES`/`PROVINCE_PAD`/`CITY_PAD`/`DISTRICT_PAD` |
| `config.txt` | 运行时配置：端口、托盘、自动开浏览器 | `PORT=5001` `TRAY=on` `OPEN_BROWSER=on` |
| `database/db_manager.py` | SQLite 连接复用、表结构、区划内存缓存、学生/结果 CRUD | **入口**: `DBManager(db_path)`；**关键**: `get_by_district_code`/`get_by_id`/`reverse_lookup`/`search_by_name`/`insert_districts`/`insert_results_batch`/`query_results`/`get_results_dataframe` |
| `database/models.py` | 轻量数据容器 | `LevelResult`/`Engine1Result`/`Engine2Result`/`MatchResult` |
| `engine/engine1.py` | 身份证前 6 位层级匹配 | **入口**: `engine1_match(db, id_card)` → `Engine1Result`；`build_codes(first6)` |
| `engine/engine2.py` | 地址解析 + 层级 3 反查 + 乡镇/村反推 | **入口**: `engine2_parse(db, address, id_card=None)` → `Engine2Result`；内部 `_lookup_county`/`_apply_county`/`_lookup_by_ancestor`/`_try_town_village_fallback` |
| `engine/decider.py` | 双引擎决策 v2 | **入口**: `dual_engine_decide(db, e1, e2)` → `MatchResult` |
| `services/match_service.py` | 单条/批量匹配编排 | **入口**: `run_match(db, id_card, address, with_detail=False)`；`process_batch(db, batch_id, on_progress=None)` |
| `services/import_service.py` | Excel 区划导入 + 学生导入，列自动识别 | **入口**: `import_district_excel(db, path, ...)`；`import_students(db, path, ...)`；`detect_student_columns`/`preview_student_file` |
| `services/export_service.py` | 结果导出 + 一进一出清理 | **入口**: `export_results(db, batch_id, status, fmt, cleanup)` |
| `utils/address_parser.py` | 地址正则解析省/市/区县/乡镇/村 | **入口**: `parse_address(address)` → dict；`_expand_province_short` |
| `utils/validators.py` | 18 位身份证格式/校验位/前 6 位提取 | `is_id_card_format`/`id_card_checksum_ok`/`get_id_first6` |
| `sdk/district_matcher.py` | 对外 SDK（自动加 sys.path） | **入口**: `DistrictMatcher(db_path).match(id_card, address)` |
| `app.py` | Flask 路由、后台任务、托盘 | `app`(Flask 实例)；`init_page`/`import_page`/`result_page`/`query_page`/`api_match`/`api_match_batch`/`export_route`/`api_import_start`/`api_import_progress`/`api_result_progress` |

### 2.3 数据层存储原理（`t_district_code` 表）

本节回答一个高频问题：**Excel 按省分 sheet（SHEET=省份），数据库为什么是 `PV-650000000000`、`pids` 逗号列表、`pid` 指向 `PV-` 这种存法？** 本质是"带冗余的邻接表"拍平模型。

#### 2.3.1 表结构（9 列）

| 列 | 含义 | 示例（北京） |
|----|------|------|
| `id` | 行主键（DB 内部 id） | 省=`PV-110000000000`；市=`fbd10556…`；区=`7e8bb18a…`（uuid） |
| `pids` | 祖先链（逗号分隔，反范式冗余） | 省=空；市=`3894082c…`；区=`3894082c…,fbd10556…` |
| `pid` | **直接父节点**的 `id`（爬树用） | 省=空；市=`PV-110000000000`；区=`fbd10556…` |
| `district_code` | 12 位国标代码（GB/T 2260，业务主键） | `110000000000` / `110100000000` / `110101000000` |
| `district_name` | 名称 | 北京市 / 市辖区 / 东城区 |
| `admin_level` | 行政层级 1–5 | `1` / `2` / `3` |
| `province_code` | 冗余：所属省代码 | `110000000000` |
| `city_code` | 冗余：所属市代码 | 省=空；市/区=`110100000000` |
| `created_at` | 入库时间 | `2026-07-27 19:31:28` |

#### 2.3.2 省级行（`PV-` 前缀）是导入时"合成"的，Excel 里根本没有这一行

Excel 按省分 sheet 时，每个 sheet 只装该省的**市、区县**（层级 ≥ 2），**不含省本身**（`import_service.import_district_excel` 逻辑）。导入时若发现 DB 缺这个省，现造一条省级行，并以 sheet 名当作省名：

```python
prov_id = "PV-" + prov_code          # PV-110000000000
all_rows.insert(0, {id: prov_id, pids:"", pid:None,
    district_code: prov_code, district_name: sheet_name,  # sheet 名当省名
    admin_level:1, province_code: prov_code, city_code:None})
# 顺手把该省下所有市级行的 pid 改成这个 PV-<code>，让父子链正确接上
for r in sheet_rows:
    if r["admin_level"] == 2:
        r["pid"] = prov_id
```

市/区行则**原样来自 Excel**（id 取 Excel 的 uuid 列，district_code/name/level 取自代码/名称/层级列）。这就是为什么你在 `t_district_code` 里看到 31 条 `PV-` 省级行 + 后面 uuid 的市/区行。

#### 2.3.3 邻接表模型（标准树结构）

整张表是一棵"全国行政区划树"拍平后的**邻接表**：每个节点只记自己的直接父 `pid`，父节点的 `id` 就是子节点 `pid` 的值。引擎爬树就是不断 `get_by_id(row["pid"])` 向上走（`engine2._lookup_by_ancestor` 沿父链核对省/市范围正是如此）。没有"children"列，因为关系全靠 `pid→id` 隐式表达——这是关系库存树的经典做法，比闭包表/路径表更易批量写入。

#### 2.3.4 `PV-` 前缀是"确定性合成 id"的关键设计

- 省级行是导入时现造的，无 Excel 给的 uuid；若给随机 uuid，后面市的 `pid` 就无法提前确定，得"先插省拿 id 再改市"两趟跑。
- `PV-` + 省代码 = **同一省永远得到同一个 id**，且 100% 可预测。市行在合成阶段直接把 `pid` 指向 `PV-<code>`，一次遍历搞定。
- 它还能**避免主键冲突**：若省用 `id=650000000000`（代码本身），而某区县 `id` 也回退成代码 `650000000000`，主键就撞了；`PV-` 前缀把省节点与普通节点彻底隔开。

#### 2.3.5 `pids`（祖先链）是反范式冗余，为了免递归

正常爬树靠 `pid` 一层层 `get_by_id` 即可。`pids` 把"省 id,市 id"直接拍平存成逗号列表（若源 Excel 自带 pids 列则原样保留）。它让"这个节点属于哪个省/市"**不查父链直接拿到**，引擎做省/市范围校验更快——典型"用空间换时间"。

#### 2.3.6 `province_code` / `city_code` 是另一层反范式冗余

不想为知道"东城区属于哪个省"而往上爬两层，直接冗余存好。`decider` 与 `engine2` 校验省/市一致性、生成导出表时直接读这两列，不做 join。

#### 2.3.7 `district_code` 是稳定业务主键（12 位）

6 位国标码自动补零成 12 位（`_norm_code`：省 2 位补 10 零、市 4 位补 8 零、区县 6 位补 6 零）。它是真实世界稳定的国标码；`id` 只是 DB 行号（导入时可变），`district_code` 不变——反查、匹配、导出都以 12 位代码为准。

#### 2.3.8 完整存储链路一句话总结

> **Excel 按省分 sheet（每 sheet 只有市/区县）→ 导入时先造一批 `PV-` 前缀的省级行并把子节点 `pid` 接上去 → 所有行一次性批量入库 → 形成一张"邻接表 + 冗余祖先链 + 冗余省/市代码"的扁平表。** 引擎匹配地址时，靠 `district_code`（12 位国标）做字典反查，靠 `pid→id` 爬树核对层级，靠 `province_code/city_code/pids` 做范围与一致性校验，全部不必递归或多表 join。

> 完整导入流程图见本文档配套图示 `district_import_flow`。

---

## 三、配置项、资源文件与外部依赖

### 3.1 配置项

#### `config.py`（程序级常量，移植时按需修改）

| 常量 | 值 | 说明 |
|------|----|------|
| `BASE_DIR` | 项目根 | 由 `__file__` 推导 |
| `DB_PATH` | `database/district_matcher.db` | SQLite 文件位置 |
| `DATA_DIR` | `data/` | 区划 Excel 存放目录 |
| `UPLOAD_DIR` | `uploads/` | 上传/导出临时目录 |
| `MUNICIPALITIES` | `{11:北京市,12:天津市,31:上海市,50:重庆市}` | 直辖市前 2 位→名称 |
| `PROVINCE_PAD`/`CITY_PAD`/`DISTRICT_PAD` | 10/8/6 个 0 | 12 位代码补零规则 |
| `ADMIN_LEVEL_NAME` | `{1:省,2:市,3:区县,4:街道/镇,5:村/社区}` | 层级显示名 |

#### `config.txt`（运行时配置，无需改代码）

| 项 | 默认 | 说明 |
|----|------|------|
| `PORT` | 5001 | Web 端口（1024–65535，越界回退 5000） |
| `TRAY` | on | 系统托盘模式（无桌面自动回退前台） |
| `OPEN_BROWSER` | on | 启动后自动打开浏览器 |

### 3.2 资源文件

| 资源 | 位置 | 必需性 | 说明 |
|------|------|--------|------|
| 区划字典 SQLite | `database/district_matcher.db` | **必需** | 引擎运行的唯一数据基础；首次空库时由区划 Excel 导入生成 |
| 区划 Excel | `data/*.xlsx` | 初始化必需 | GB/T 2260 全国行政编码数据，多 sheet 按省分表 |
| 导出文件 | `uploads/result_export_*.{xlsx,csv}` | 运行产出 | 文件名含 batch_id + 时间戳 |
| 托盘图标 | 内存生成（Pillow） | 可选 | 无外部图片文件依赖 |

### 3.3 外部依赖

`requirements.txt`：

| 依赖 | 用途 | 是否引擎核心必需 |
|------|------|------------------|
| `flask>=3.0` | Web 框架 | 否（仅接入层） |
| `pandas>=2.0` | Excel 读写、DataFrame 导出 | 否（仅导入/导出层） |
| `openpyxl>=3.1.5` | xlsx 引擎（pandas 依赖） | 否（仅导入/导出层） |
| `pystray>=0.19` | 系统托盘 | 否（仅桌面可选） |
| `Pillow>=10.0` | 托盘图标生成 | 否（仅桌面可选） |

> **引擎核心**（engine + decider + address_parser + validators + db_manager + config）**仅依赖 Python 标准库**（`sqlite3`/`re`/`dataclasses`/`threading`）。pandas/flask 仅在接入层与导入导出层使用，可剥离。

---

## 四、关键算法与核心逻辑

### 4.1 引擎①：身份证前 6 位层级匹配

**设计意图**：身份证前 6 位即 GB/T 2260 的 6 位区县码，补零为 12 位后按省(2)/市(4)/区县(6) 三级查库。

**实现**（`engine1.py`）：
1. `validators.get_id_first6` 提取前 6 位（非数字→error）。
2. `build_codes`：省码=前2位+10零，市码=前4位+8零，区县码=前6位+6零。
3. 逐级 `db.get_by_district_code(code, level)` 查库。
4. **直辖市特判**：前 2 位命中直辖市时，市级直接取省级本身（真实数据直辖市有 level-2 的"市辖区"虚拟节点，需跳过）。
5. 区县若命中"市辖区"虚拟名则视为未命中。

**适用场景**：身份证号合法且其签发地代码现行有效时，三级精准命中。**局限**：无法处理已撤销的旧代码（如 230105 太平区），此时层级 3 未命中，需引擎②兜底。

### 4.2 引擎②：地址文本解析与反查

**设计意图**：地址是主信息源，能正确反映现行归属。引擎②先用正则提取省/市/区县名称，再用区划字典反查 12 位代码，并支持乡镇/村反推。

**实现**（`address_parser.py` + `engine2.py`）：

**地址解析**（`parse_address`）：
1. **自治区简称展开**：入口将 `内蒙古/广西/西藏/宁夏/新疆` 展开为全称，使省正则正确剥离前缀（避免"宁夏彭阳县"被区县正则整体捕获）。
2. 省级：`MUNICIPAL_RE`（4 直辖市）→ `PROVINCE_RE`（省/自治区）。
3. 市级：剔除省前缀后用 `CITY_RE`（市/自治州/地区/盟）匹配；直辖市市级=省级。
4. 区县级：剔除省/市前缀后用 `DISTRICT_RE`（自治县/区/县/旗/新区）匹配；`(?<!社)区` 防止"社区"误匹配。
5. 乡镇/村：在剔除区县名后的剩余文本中用 `TOWN_RE`/`VILLAGE_RE` 抽取，避免长区县名（如"莫力达瓦达斡尔族自治旗"）被截断。

**反查**（`engine2_parse`）五步：
1. 市级反查；若"市"实为县级市（省略地级市），作为层级 3 处理并推导父级市/省。
2. 区县名反查（支持历史别名 `_HISTORICAL_NAME_MAP` 旧名→新名）；优先在已知市范围内。
3. 兜底扫描地址中 `XX市/县/区/旗` 候选。
4. 乡镇/村辅助反推：`_lookup_by_ancestor` 沿父链向上核对省/市归属范围，避免同名跨区误匹配。
5. 补全省级代码。

**关键函数**：
- `_lookup_county(db, name, city_pid)`：历史别名映射 + 名称反查层级 3。
- `_apply_county(db, res, county_row, city_pid_holder)`：填充 district 并向上推导 city/province；直辖市/省直管县保留 city 名称。
- `_lookup_by_ancestor(db, name, level, scope_level, scope_id)`：乡镇/村沿父链核对省/市 scope。

**适用场景**：地址文本规范或半规范（含省/市/区县关键词），能正确推导现行代码，是历史更名的天然补救。

### 4.3 决策器 v2

**设计意图**：以引擎②层级 3 为锚点，引擎①仅做校验与兜底，杜绝跨引擎省市区县拼接。

**实现**（`decider.py` `dual_engine_decide`）：

```
e2_l3 = e2 有 district_code（主锚点）

A. e2 层级3命中 → 采用引擎②全量
   ├ 引擎①省/市名称一致 → 正确
   ├ 引擎①省/市冲突   → 需复核（仍输出引擎②结果）
   └ 引擎①无区县可校验 → 已解析

B. e2 仅省/市 + e1 区县命中 → 已解析（地址省市+证件区县）
C. e2 抽出区县名但无代码   → 需复核
D. e2 无输出 → 纯引擎①兜底
   ├ e1 区县命中 → 已解析(仅证件)
   ├ e1 仅省/市 → 部分
   ├ e1 异常    → 异常
   └ 全无       → 无匹配
```

**关键设计**：
- 省/市校验用**名称比对**（非代码前缀），天然兼容直辖市市代码表示差异（1100 vs 1101）。
- 冲突时不改写引擎②结果，仅置"需复核"供人工核查。

**状态枚举（v2）**：`正确 / 已解析 / 已解析(仅证件) / 需复核 / 部分 / 无匹配 / 异常`。

### 4.4 区划字典内存缓存

**设计意图**：区划字典只读且量小（约 66 万行），匹配热路径每条学生都查库，内存缓存消除重复 IO。

**实现**（`db_manager.py`）：
- 首次访问 `_ensure_cache` 全量载入 `t_district_code` 到 4 个字典：`_by_code[(code,level)]`/`_by_id`/`_by_name_level[(name,level)]`/`_by_name`。
- `get_by_district_code`/`get_by_id`/`reverse_lookup`/`search_by_name` 全走内存 O(1)/O(k)。
- `insert_districts`/`reset_db` 后 `_invalidate_cache` 失效重载。
- 写操作（学生/结果）走 SQLite，不进缓存。

### 4.5 历史别名与县级市省略市

**设计意图**：兼容地址中的旧县名与省略地级市的县级市写法。

- `_HISTORICAL_NAME_MAP`（15 条）：旧名→新名（库仅存新名，旧名翻译后反查）。如 `嫩江县→嫩江市`、`双城市→双城区`。
- 县级市省略市：地址层级 3 为"XX市"（县级市）省略层级 2 时，引擎②显式走"识别县级市为层级 3 → 向上补父级市/省"路径（用户强调项）。

### 4.6 一进一出数据生命周期

**设计意图**：学生数据为临时流水，导出后自动清理，数据库仅长期保留区划字典。

- `t_district_code`（区划字典）= 参考数据，长期保留。
- `t_student`/`t_result`（学生流水）= 临时数据，`export_results(cleanup=True)` 整批导出后 `db.delete_batch` 清除。
- `process_batch` 重跑前先 `delete_results_by_batch` 覆盖旧结果，避免新旧混入。

---

## 五、移植与适配指南

### 5.1 必须适配/修改的部分

| 项 | 位置 | 适配说明 |
|----|------|----------|
| **区划数据源** | `data/*.xlsx` | 必须提供符合 GB/T 2260 的区划 Excel（列：代码/名称/层级/id/pid）。若列头不同，调整 `import_service` 的 `keyword_map` 关键词。 |
| **DB 路径** | `config.DB_PATH` | 按目标部署调整 SQLite 文件位置。 |
| **直辖市表** | `config.MUNICIPALITIES` | 中国行政区划固定，无需改；移植到其他国家/层级体系时需重写。 |
| **代码补零规则** | `config.*_PAD` | 12 位 GB/T 2260 专用。若目标体系位数不同，需同步修改 `build_codes` 与 `_norm_code`。 |
| **历史别名表** | `engine2._HISTORICAL_NAME_MAP` | 与目标数据的更名历史强相关，需按实际更名记录维护。 |
| **自治区简称表** | `address_parser._PROVINCE_SHORT_MAP` | 中文地址特有，移植到其他语言/国家需删除或重写。 |
| **地址正则** | `address_parser.*_RE` | 中文行政区划后缀（省/市/县/区/旗/镇/村）专用，移植需重写。 |
| **身份证校验** | `validators.py` | 中国 18 位身份证 GB 11643 专用，移植需替换为目标证件体系。 |

### 5.2 可直接复用的部分

| 部分 | 复用价值 |
|------|----------|
| 双引擎 + 决策器架构 | "多源信息融合 + 主引擎锚点 + 校验兜底"模式可推广到任何多源归属判定 |
| `DBManager` 内存缓存模式 | 只读字典全量载入内存 + 写操作走 DB 的思路通用 |
| 一进一出数据生命周期 | 临时流水导出即清理的设计通用 |
| 后台任务 + 进度轮询 | `_start_process_task`/`_start_import_task` + 进度 API 模式通用 |
| 决策器分支设计 | "命中→校验一致/冲突/兜底"的状态机思路通用 |
| `_lookup_by_ancestor` | 沿父链核对 scope 的反查思路适用于任何树形层级数据 |

### 5.3 兼容性风险

| 风险 | 影响 | 缓解 |
|------|------|------|
| **区划数据不含省级行** | 部分数据集按省分 sheet 从 level-2 起 | 已通过"缺失省级自动合成（以 sheet 名为省名）"处理，移植时验证数据集是否含 level-1 |
| **直辖市"市辖区"虚拟节点** | 引擎①市匹配会误命中 | 已特判跳过"市辖区"；移植时确认数据集中直辖市层级结构 |
| **身份证签发地已撤销** | 引擎①层级 3 未命中 | 引擎②兜底；移植时确保字典含现行代码 |
| **地址书写不规范** | 简称/省略市/旧地名 | 已处理自治区简称、县级市省略市、历史别名；极端不规范地址可能落"需复核" |
| **SQLite 并发** | 单连接 + 写锁，高并发写受限 | `check_same_thread=False` + `threading.Lock`；超大规模需换 DB 或连接池 |
| **内存缓存占用** | 66 万行全量载入约占数百 MB | 大字典场景需评估内存；可改为按需加载或分片 |
| **正则歧义** | "社区"含"区"、"宁夏"被吃进区县名 | 已用 `(?<!社)区`、简称展开、剔除前缀后抽取缓解；新地址模式需持续维护正则 |
| **托盘跨平台** | pystray/Pillow 在无桌面环境不可用 | 已 try/except 回退前台；服务器部署用 `--no-tray` |
| **pandas 版本** | `to_dict("records")`/`read_sql_query` API | 锁定 `pandas>=2.0`；低版本需验证 |

---

## 附：关键状态枚举与决策规则汇总表（v2）

| 优先级 | 引擎②层级3 | 引擎①区县 | 省/市校验 | 输出代码来源 | 状态标识 |
|--------|-----------|----------|----------|-------------|---------|
| A1 | ✅命中 | ✅命中 | 名称一致 | 引擎②全量 | 正确 |
| A2 | ✅命中 | ✅命中 | 省/市冲突 | 引擎②全量 | 需复核 |
| A3 | ✅命中 | ❌无区县 | - | 引擎②全量 | 已解析 |
| B1 | ❌无代码 | ✅命中 | - | 引擎②省市+引擎①区县 | 已解析 |
| C1 | 仅区县名 | - | - | 引擎②(无代码) | 需复核 |
| D1 | ❌无输出 | ✅命中 | - | 引擎①全量 | 已解析(仅证件) |
| D2 | ❌无输出 | ❌仅省/市 | - | 仅省/市 | 部分 |
| D3 | ❌无输出 | 异常 | - | - | 异常 |
| D4 | ❌无输出 | ❌无 | - | - | 无匹配 |

---

*文档版本：v2.2（对应 AGENTS.MD v2.2）· 新增 2.3 数据层存储原理（t_district_code）与导入流程图 · 生成日期：2026-07-27*
